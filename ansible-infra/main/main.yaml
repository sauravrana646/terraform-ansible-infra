- name : Configure basic setup on all nodes
  hosts : "{{ hosts }}"
  gather_facts : no
  remote_user : "{{ remote_user }}"
  become : yes
  vars_files:
  - ./tf-ansible-vars.yml
  tasks:
  - name: pinging
    ping:
  # roles : 
  # - daffo-ec2-server
